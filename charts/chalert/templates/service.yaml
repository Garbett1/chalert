apiVersion: v1
kind: Service
metadata:
  name: {{ include "chalert.fullname" . }}
  labels:
    {{- include "chalert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chalert.selectorLabels" . | nindent 4 }}
